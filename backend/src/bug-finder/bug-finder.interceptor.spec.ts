import { BugFinderInterceptor } from './bug-finder.interceptor';

describe('BugFinderInterceptor', () => {
  it('should be defined', () => {
    expect(new BugFinderInterceptor()).toBeDefined();
  });
});
